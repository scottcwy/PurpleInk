开发启动：`pnpm dev:all` 通过 PowerShell 脚本同时启动 Next.js dev server 与 purpleink-server worker；数据库迁移 `pnpm db:migrate` 执行 scripts/setup/db-migrate.ts；端到端验证 `pnpm verify:e2e` 调用 e2e-smoke.ts；PostgreSQL 测试 `pnpm test:pg` 使用 vitest.pg.config.ts；CVC 取证构建可通过 CVC_NEXT_DIST_DIR 指定独立 .next 目录避免争用。