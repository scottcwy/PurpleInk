- 所有外部敏感信息通过 Docker Compose secrets 或 `.env.local` 注入，示例文件统一以 `.example` 后缀命名且不包含真实值。
- 环境变量采用 `CVC_` 前缀命名（如 `CVC_BASIC_AUTH_USER`、`CVC_ALLOWED_CIDRS`），集中管理反向代理相关配置。
- 容器入口脚本使用 `set -eu` 严格模式，并在缺少关键 secrets 时显式退出而非降级运行。