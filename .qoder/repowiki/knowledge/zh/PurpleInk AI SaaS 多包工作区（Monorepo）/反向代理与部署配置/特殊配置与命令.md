生成 Basic Auth 哈希需运行 `docker run --rm caddy:2.10-alpine caddy hash-password --plaintext '<口令>'`，将输出与用户名拼成单行写入 `deploy/reverse-proxy/secrets/basic_auth_credentials`（该文件已 gitignore，仅通过 Compose secrets 在运行期挂载到 `/run/secrets/`）。