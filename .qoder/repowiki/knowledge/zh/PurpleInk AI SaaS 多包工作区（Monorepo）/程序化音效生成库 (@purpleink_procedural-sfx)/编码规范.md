- 所有对外暴露的字符串字面量枚举（preset、mode、status）均以 `as const` 数组定义并通过 `(typeof ...)[number]` 派生类型，保证编译期穷尽检查。
- 输入参数在函数入口处通过显式类型守卫或 `throw new TypeError/RangeError` 进行 fail-closed 校验，而非依赖调用方保证。
- 确定性输出通过 JSON.stringify 后传入 `sha256Hex` 生成 64 位十六进制哈希，timingHash 与 cuePlanHash 成对出现且允许同为 null。
- WAV 合成使用固定常量（SAMPLE_RATE=48000、CHANNELS=1、BITS_PER_SAMPLE=16、HEADER_BYTES=44）并在 `readWavInfo` 中严格校验 RIFF/WAVE/fmt /data 四段头字段。
- 测试文件与源文件同名并存（`*.test.ts`），由 vitest 自动发现执行。