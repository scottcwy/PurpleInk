- 每个 Issue 以 `ISSUE-NNN-标题.md` 命名，包含结论、断点分析、依赖图、验收标准与证据目录，可独立派发给执行者。
- 证据文件统一放在 `docs/issues/evidence/<issue-id>/` 下，按类型分 `auth/`、`fixtures/`、`p2/p3/p6/p7/` 等子目录，配合 README.md 说明。
- 计划文件采用 `PLAN-NNN-标题.md` 或 `motion-interaction/00-README.md` 序列编号，明确对应上游 ISSUE 与审查基线 commit。
- 文档间通过显式交叉引用（如 `docs/conventions/routing.md §9`、`AGENTS.md §0`）建立依赖，而非隐式链接。
- 所有变更必须附带可验证的证据（截图 SHA-256、JSON 快照、HTTP 响应、docker compose config 输出），禁止仅文字描述。