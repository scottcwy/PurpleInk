Node.js fs/promises 写盘；通过 `../lib/step-client` 调用 StepFun 多模态 API（默认模型 `step-3.7-flash`，环境变量 `STEP_VISION_MODEL` 覆盖）；浏览器侧使用原生 DOM API（`document.styleSheets`、`getComputedStyle`、`querySelectorAll`）提取品牌信息。