- 对外暴露的函数均从 `index.ts` 统一 re-export，调用方只依赖单一入口路径。
- 可选字段采用「有值才写入」策略（如 `buildTokens` 中仅追加非空数组/对象），避免污染下游判断。
- 文本处理统一先做空白折叠（`/\s+/g` 替换为单空格）再 trim，保证输出稳定。
- 列表类数据在收集后统一做去重（Set 记录已见 key）并按出现频次或大小限制切片（如 colors、logos、pricing、stats）。
- 外部服务调用（StepFun）包裹 try/catch 并记录 warn 日志，失败时回退到本地派生结果，保证适配器鲁棒性。