- 所有异步阶段通过 onPhase(phase) 回调上报进度（queued/capturing/composing/rendering/verifying/muxing/done），供上层追踪状态
- 对外暴露的入口函数统一接受 options 对象并支持 AbortSignal，在关键步骤前调用 signal.throwIfAborted()
- 错误路径统一记录结构化日志（logger.info/logger.error）并在 LLM 失败时自动降级到模板路径
- 项目目录结构遵循 HyperFrames 规范：index.html + hyperframes.json + meta.json + package.json + assets/ 子目录