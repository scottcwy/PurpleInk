需设置 `STEP_API_KEY`（必填）、可选 `STEP_VISION_MODEL` 覆盖视觉模型；启用 IMAP 自助注册需配置 `IMAP_HOST/IMAP_PORT/IMAP_SECURE/IMAP_USER/IMAP_PASSWORD`（QQ 邮箱授权码）；通过 `BROWSER_DRIVER=mock` 可跳过浏览器安装进行链路冒烟；publicOnly 模式需配合 `../security/public-url-policy` 的公网白名单策略。