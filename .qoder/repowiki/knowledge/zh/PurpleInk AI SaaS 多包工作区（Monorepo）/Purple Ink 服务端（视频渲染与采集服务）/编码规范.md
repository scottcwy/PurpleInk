- 所有子模块通过 lib/logger 统一记录结构化日志，并通过 lib/error-message 对敏感信息进行脱敏
- 环境变量统一由 lib/load-env 从 .env 与 .env.local 加载，服务启动时即完成注入
- 对外 API 与内部集成通过 server/internal-api.ts 与 internal-auth.ts 进行鉴权隔离
- 异步任务统一经 job-runner 入队、job-store 持久化，避免直接跨模块调用