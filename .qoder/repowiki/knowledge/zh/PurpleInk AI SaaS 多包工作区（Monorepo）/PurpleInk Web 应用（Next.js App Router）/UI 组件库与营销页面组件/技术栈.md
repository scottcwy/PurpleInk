React + TypeScript，使用 `motion/react`（Framer Motion）驱动按钮按压、覆盖层入场/出场动画；样式基于 Tailwind CSS 与设计系统变量（`ds-*` 前缀）；Next.js App Router 客户端组件（`'use client'`）；Toast 状态通过模块级单例 store 配合 `globalThis.crypto.randomUUID()` 生成 ID。