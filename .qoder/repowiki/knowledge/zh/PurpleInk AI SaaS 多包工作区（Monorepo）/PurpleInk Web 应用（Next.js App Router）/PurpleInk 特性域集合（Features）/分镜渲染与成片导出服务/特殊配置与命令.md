测试数据位于 `__fixtures__/` 下的 HTML 片段（如 `deterministic-shot.html`、`invalid-runtime.html`、`runtime-page-error.html` 等），供 `*.test.ts` 模拟渲染场景；部分集成测试使用 `.pg.test.ts` 后缀并依赖 PostgreSQL fixture（`render.pg-fixture.ts`）。