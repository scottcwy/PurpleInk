TypeScript + Drizzle ORM（`drizzle-orm`）操作 PostgreSQL；Node.js 原生 `crypto`、`fs/promises`、`os`、`path` 用于哈希、临时目录与路径；FFmpeg 通过 `encode.ts`/`concat.ts` 调用进行 MP4 编码与拼接；存储抽象为 `StorageAdapter`（由 `@/lib/storage` 提供）；队列通过 `@/lib/queue` 的 `QueueAdapter` 解耦。