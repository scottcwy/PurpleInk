- 每个子模块通过 index.ts 集中 re-export 公共 API，内部实现文件不被外部直接引用
- 数据库相关测试统一使用 .pg.test.ts 后缀并配合 fixture 文件运行
- 服务类或仓储接口以 Dependencies 类型作为依赖注入参数，避免全局单例
- 错误类型集中定义并在 index.ts 中导出，供调用方按 code 分类处理
- 类型定义与值导出分离，类型通过 type 关键字显式导出