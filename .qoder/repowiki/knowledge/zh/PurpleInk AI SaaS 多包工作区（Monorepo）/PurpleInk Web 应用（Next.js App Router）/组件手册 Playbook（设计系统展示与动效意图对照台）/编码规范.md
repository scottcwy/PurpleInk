- 每个分类页通过 `entriesByCategory(category)` 或 `countMotionIntents(status)` 等纯函数从中央登记表读取数据，页面本身不硬编码列表。
- Demo 组件统一以 `<ComponentName>Demo</ComponentName>` 命名并从 `@/components/ui/<name>.demo` 或 `@/features/*/collapsible-panel.demo` 导入，再由 registry 聚合。
- 动效意图以 `id` 作为键在 `motion-intents.ts` 与 `intent-specimens.tsx` 的 `SPECIMENS` 字典之间保持严格一致，缺键时对照台显式提示而非静默跳过。
- 所有页面遵循统一的布局模板：外层 `ds-app-gradient text-ds-text min-h-screen p-5 sm:p-8`，内容包裹在 `mx-auto max-w-{5,6}xl` 容器中，顶部保留返回 `/playbook` 的链接。
- 分类标题与区块头部统一使用 `font-mono text-[11px] font-semibold tracking-wide uppercase` 的 muted 标签风格。