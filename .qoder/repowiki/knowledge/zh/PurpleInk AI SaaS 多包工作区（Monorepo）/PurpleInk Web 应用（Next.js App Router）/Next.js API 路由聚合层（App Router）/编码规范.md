- 每个业务域独立目录，route.ts 与同名的 route.test.ts 并列存放
- 受保护路由统一使用 withApiSession 包装器获取 session 或 userId，避免在每个 handler 中重复鉴权逻辑
- 所有 handler 显式设置 dynamic = 'force-dynamic' 以禁用静态生成
- 输入参数通过 URL searchParams 解析并使用本地 isXxx 守卫函数做类型收窄与校验
- 错误响应统一返回 { ok: false, error } 结构并附带 HTTP 状态码