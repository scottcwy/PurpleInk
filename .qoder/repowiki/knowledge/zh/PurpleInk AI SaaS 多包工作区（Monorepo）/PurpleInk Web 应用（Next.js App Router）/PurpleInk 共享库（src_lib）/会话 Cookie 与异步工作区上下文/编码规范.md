- 所有对外导出的常量（如 `SESSION_COOKIE_NAME`、`SESSION_TTL_MS`、`SYSTEM_USER_ID`）采用 UPPER_SNAKE_CASE 命名并置于文件顶部集中声明。
- 对缺失上下文的访问一律抛错而非返回 null 或默认值，确保遗漏建立上下文会立即失败而非静默串号。
- 服务端专用代码通过 `import 'server-only'` 显式标记，防止被客户端打包引入。
- cookie 安全属性统一通过 `baseAttributes()` 函数集中构造，再在设置/清除处展开复用。