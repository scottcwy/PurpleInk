- 所有对外暴露的配置对象均使用 `Object.freeze` 冻结，确保运行时不可变。
- 版本常量通过 `satisfies` 与对应接口绑定，保证字段完整性与顺序一致性。
- 类型守卫函数采用 TypeScript 类型谓词形式（如 `value is ProjectWorkflowKind`），在运行时同时完成校验与类型收窄。
- 每个源文件都配有同名 `.test.ts` 文件，使用 Vitest 的 `describe/it/expect` 结构对常量值与序列化输出做精确断言。