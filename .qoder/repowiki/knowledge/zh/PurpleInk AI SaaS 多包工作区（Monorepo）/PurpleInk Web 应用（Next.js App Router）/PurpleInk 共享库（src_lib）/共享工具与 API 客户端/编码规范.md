- 所有公共能力以命名导出函数/类型形式暴露，不使用类或模块级可变状态。
- API 客户端统一通过 fetch + JSON 序列化发起请求，并在非 ok 响应时抛出包含错误信息的 Error。
- 类型守卫使用 TypeScript 类型谓词函数（如 isThemeMode）配合 as const 常量数组进行运行时校验。
- UI 副作用（如 DOM class 切换、文件下载）集中在独立函数中，保持纯函数与副作用分离。