- 每条规则以 { id, pattern: RegExp, message } 对象形式声明在 DETERMINISM_RULES 数组中，id 为短标识符（如 raf、date-now、math-random）。
- 扫描函数按行拆分源码并逐行对所有规则执行 pattern.test()，命中即构造包含 ruleId/message/line/snippet 的违规对象。
- 对外同时提供新旧两套 API：checkSource 为新 Tool 边界首选，checkDeterminism 作为别名保持向后兼容，isDeterministic 提供布尔快捷判断。