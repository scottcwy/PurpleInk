开发需同时启动前端与 worker：`pnpm dev:all` 调用 scripts/dev/start-dev.ps1 并行启动 Next dev 与 server dev；数据库迁移 `pnpm db:migrate` 执行 scripts/setup/db-migrate.ts；PostgreSQL 初始化脚本在 scripts/setup/postgres-init.sql；测试分两类：普通测试 `pnpm test`，带 PostgreSQL 的集成测试 `pnpm test:pg`（使用 vitest.pg.config.ts）。