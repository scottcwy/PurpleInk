本地开发：`pnpm dev:all` 一键拉起完整栈，`pnpm dev:status` 仅体检，`pnpm dev:stop` 释放端口；凭据引导：`pnpm setup:bootstrap-credentials` 需先运行 `pnpm db:migrate`；迁移：`scripts/migration/export-sqlite.ts --snapshot <file> --backup-report <report> --out <dir>` 与 `scripts/migration/import-postgres.ts --manifest <file>`；验证：`pnpm verify:e2e --script docs/issues/evidence/fixtures/smoke.txt`。