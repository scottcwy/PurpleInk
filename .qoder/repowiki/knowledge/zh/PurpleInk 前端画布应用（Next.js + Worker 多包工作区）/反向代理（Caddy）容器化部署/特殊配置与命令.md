生成密码哈希的命令：`docker run --rm caddy:2.10-alpine caddy hash-password --plaintext '<口令>'`；将输出与用户名拼成单行写入 `deploy/reverse-proxy/secrets/basic_auth_credentials`（该文件已被 gitignore 忽略，需配合 Compose `secrets:` 在运行期挂载到 `/run/secrets/basic_auth_credentials`）。