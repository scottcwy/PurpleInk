- 敏感凭据通过 Docker Compose secrets 挂载到 `/run/secrets/` 路径，而非以明文文件或环境变量形式硬编码进镜像层。
- 启动脚本在缺少必要凭据文件时直接 `exit 1` 终止，遵循 fail-fast 原则，禁止以无认证状态运行。
- 环境变量采用 `CVC_` 前缀命名（如 `CVC_BASIC_AUTH_USER`、`CVC_ALLOWED_CIDRS`），并通过 `${VAR:=default}` 语法提供默认值。