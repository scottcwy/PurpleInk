开发需同时启动前后端：`pnpm dev` 启动 Next.js，`pnpm dev:worker` 启动渲染后端，或使用 `pnpm dev:all` 一键启动。数据库迁移执行 `pnpm db:migrate`，类型检查用 `pnpm typecheck`，单元测试 `pnpm test`，PostgreSQL 集成测试 `pnpm test:pg`。生产构建通过 Dockerfile 打包，反向代理由 deploy/reverse-proxy/Caddyfile 配置。