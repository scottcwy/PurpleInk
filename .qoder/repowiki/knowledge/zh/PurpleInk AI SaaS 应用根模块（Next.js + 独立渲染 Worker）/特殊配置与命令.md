开发需同时启动前端与 Worker：`pnpm dev:all`（PowerShell 脚本协调两个进程）或分别执行 `pnpm dev` 和 `pnpm dev:worker`。数据库初始化通过 `pnpm db:migrate` 执行 Drizzle 迁移，PostgreSQL 初始化 SQL 位于 `scripts/setup/postgres-init.sql`。E2E 验证使用 `pnpm verify:e2e`，架构一致性校验使用 `pnpm verify:v3`。