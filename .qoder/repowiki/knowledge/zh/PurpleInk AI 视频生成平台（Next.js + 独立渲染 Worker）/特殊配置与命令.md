开发需同时启动前端与 worker：`pnpm dev:all`（PowerShell 脚本 scripts/dev/start-dev.ps1 管理双进程）；数据库迁移 `pnpm db:migrate`；Postgres 测试 `pnpm test:pg`；架构一致性检查 `pnpm verify:v3`；生产构建前需设置 BACKEND_ORIGIN 指向渲染 worker。Node 引擎要求 >=22.11.0。