开发需同时启动前端与 worker：`pnpm dev:all`（PowerShell 脚本并行启动 next dev 与 dev:worker），或分别执行 `pnpm dev` 和 `pnpm dev:worker`。数据库迁移通过 `pnpm db:migrate` 调用 scripts/setup/db-migrate.ts；PostgreSQL 集成测试用 `pnpm test:pg` 加载 vitest.pg.config.ts；架构一致性校验 `pnpm verify:v3` 与 `pnpm report:v3` 调用 scripts/verify/v3-architecture.ts。