# 示例文件，不含真实凭据。复制为同目录下的 `basic_auth_credentials`
# （该文件名已加入 .gitignore，不会被提交）并替换成真实值。
#
# 生成哈希：
#   docker run --rm caddy:2.10-alpine caddy hash-password --plaintext '你的口令'
#
# 把输出的哈希与用户名拼成单行，格式："<用户名> <bcrypt哈希>"，例如：
# admin $2a$14$examplehashvaluereplaceme000000000000000000000000000000
