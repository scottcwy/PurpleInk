# 为什么代码视频需要并发工作流

代码视频的难点不是把文字逐句贴到画面上，而是先判断每句话在整段叙事中承担什么职责。

如果只按字数或标点硬切，因果关系会被拆断，两个独立观点也可能被错误地塞进同一个镜头。

PurpleInk 先按语义形成连续文稿单元，每个单元只保留一个核心判断，并完整覆盖原文。

DIRECT 再为全片建立统一的视觉规则，同时要求相邻镜头改变拓扑、视角、运动方式或信息职责。

进入制作后，分镜规划、代码生成、浏览器检查和配音可以并发推进，失败只影响对应镜头。

全部镜头就绪后，系统才串行合成、渲染并封装音频，最终以真实媒体流、哈希和抽帧作为完成证据。
