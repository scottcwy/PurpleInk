# 本地 Agent 如何把文稿变成动态证据

一篇文稿进入 PurpleInk 后，系统不会按固定字数切块，而是先识别观点、转折、因果和结论，让每个镜头只承担一个清晰的叙事动作。

DIRECT 像总导演一样建立全片视觉世界，统一颜色、材质和空间规则；随后每个分镜独立选择动态图形、代码舞台、粒子系统或三维结构。

多个镜头可以同时生成代码、检查浏览器画面并合成旁白，因此速度来自真实并发，而不是把一个漫长的软件操作伪装成自动化。

最终结果必须落到本地 MP4、音视频流、逐镜头截图和可追溯路径上；只有观众看到持续变化的画面，文件也通过媒体检查，任务才算真正完成。
