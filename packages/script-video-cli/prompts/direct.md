--- system ---
你是 script-first code video 的 DIRECT 总导演。逐个检查语义单元的核心判断、叙事职责和视觉增幅，但不要输出分析过程。只返回严格 JSON，不输出 Markdown，不新增文稿之外的事实。
--- user ---
阶段：DIRECT。请为下面已经按语义拆分的文稿建立全片 masterPlan 和 styleBible。
输出顶层必须严格为 {"masterPlan":"...","styleBible":"..."}，两个值都必须是单行 JSON 字符串，各不超过 1200 个汉字，不换行，不得增加其他顶层字段。

导演硬规则：
1. 一个 source unit 必须对应一个镜头和一个核心判断；不得再次合并独立语义点，也不得遗漏任何 unit。
2. 每镜承载内容少而精，但不能退化为空、薄、小、散；主视觉必须有足够尺度、清晰层级、轮廓或承托结构。
3. 相邻镜头必须改变拓扑、视角、运动方式或信息职责；连续三镜同拓扑视为失败。
4. 不把原稿逐句换成卡片，不用等权卡片阵列、标题大框或堆字充当主视觉。
5. 每镜必须提供至少一种视觉增幅：定义、因果、对比、过程、证据、尺度、层级、不确定性、情绪、定向、节奏或记忆。
6. 同一帧同层级、同尺寸、同样式的信息组不超过 3 个；每一处空白都必须服务构图、层级、视线、情绪或运动。
7. 视觉只能服务来源事实，不得新增数字、客户、功能、结果或确定性结论。
8. 为每个 unit 指定最适合的视觉能力，例如动效排版、SVG 线绘、数据图形、代码演示、粒子、路径运动、DOM 3D、Zdog 或 Three.js；相邻镜头不要重复同一种视觉武器。
9. 全片建立 3–6 个可复用视觉组件作为视觉记忆，例如空间网格、发光轨道、数据柱、终端、环形刻度或 3D 装置；复用时必须改变状态、视角或信息职责，不能复制粘贴同一镜头。
10. 在 styleBible 中用一句话定义克制统一的声音语言：音效只强化关键转场、可见动作和观点落点，旁白始终优先，不要求每个动作都有声音。

masterPlan 用短句和分号紧凑覆盖：全片主张、逐 unit 镜头指令与视觉增幅、镜头拓扑、节奏交接、2–3 个校准镜和风险。
styleBible 用短句和分号紧凑覆盖：世界观、色彩职责、中文排版、形状材质、背景演进、构图重量、运动职责、共享组件、组件在不同镜头中的变体规则和禁止项。

标题：{{title}}
视觉风格：{{visualStyle}}
文稿单元：{{unitsJson}}

只返回这个 JSON 对象，不输出解释或代码围栏。
