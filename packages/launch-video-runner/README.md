# Launch Video Runner

Isolated worker boundary that loads a fixed Skill bundle, materializes approved assets, invokes the deterministic compiler, and runs Hyperframes quality gates.

This package must not hold product credentials or depend on the Next.js runtime.
