# ISSUE-015 P-3 · 成片旁白与硬字幕验收证据

日期：2026-07-26

## 结论

P-3 已选择并完成“最终导出阶段统一媒体装配”。新 `final-mp4`
登记为 `cvc.final-video/v2`，包含 H.264 视频、AAC 旁白和烧录硬字幕；
旧 `v1` 静音成片不覆盖。实现未修改 `server/**`，未改写
`audioAllocation.durationInFrames`，也未使用 `atempo`。

## 完整历史项目复用

- 项目：`30c5e856-a8e0-492b-9c90-ff5f6786de72`
- 就绪：旁白 7/7，字幕 7/7，阻塞项 0。
- 视频：H.264，1920×1080，2732 帧，时长 `91.066667s`。
- 音频：AAC，48 kHz，立体声，时长 `91.066000s`。
- 音画时差：`0.000667s`，小于一帧。
- 两次相同输入导出 SHA-256：
  `deca119d2cfcfad813af644569e038fb6a19bf47f8bf80a6a49d485fd885b69d`。
- 重新导出前后旁白与字幕 Artifact 数量均保持 7/7，没有重跑 TTS/ASR。
- 按每镜有效 cue 中点抽取 7 帧，逐帧目视确认中文无乱码和豆腐块、
  字幕不超过两行且未越过底部安全区。

## 极小真实文本到成片 E2E

- 项目：`36375939-ce71-4331-9a5d-31402737a82b`
- 输入：35 个字符，真实经过 Director、TTS/字幕、代码生成、单镜渲染、QA 与导出。
- 结果：全部 9 个图节点 `succeeded`，失败节点 0。
- 媒体就绪：旁白 1/1，字幕 1/1，`delivery=narration-hard-subtitle-v2`。
- 最终 Artifact：`33d32b26-b9f6-45d1-ba2e-150dacfad723`。
- SHA-256：
  `272845b34f74d0df2b8b1bcb671b8a63eb27ac9f36c5f46ac6e738ff93266ed3`。
- 视频：H.264，206 帧，时长 `6.866667s`。
- 音频：AAC，48 kHz，立体声，时长 `6.866000s`。
- 音画时差：`0.000667s`，小于 30 fps 的一帧。
- 中点抽帧确认原稿中文硬字幕清晰、两行内、未越安全区。

E2E 报告的 `ok=true`；其中持续写入的 `pi-session` 属实时日志，
报告已按既有规则从不可变 Artifact 哈希判定中排除。

## 代码与测试范围

- 可信媒体装配与 API 就绪合同：`c2fc524`
- 原稿字幕规范化、时间轴和 ASS：`2f47aae`
- 一次 ffmpeg 完成旁白、硬字幕与视频：`583d0e3`
- 导出 UI 展示真实媒体状态：`bbbe91e`

覆盖了跨 lane/unit/hash 错配、文件缺失、字幕漂移与非法时间轴、
ASS 转义、无 `atempo`/`-an`、ffmpeg 失败不登记 v2、API 409 兼容字段、
UI 不伪造 BGM/SFX，以及真实媒体确定性导出。
