# predev 集成总账（2026-08-01）

> **HISTORICAL（2026-08-02 已被取代）**：本文是 2026-08-01 三分支预集成的历史
> 账本，只供追溯；文中镜像 / 反向代理 / 编排栈等部署主张不再代表现行实现。
> 当前部署真值见 `docs/deployment/zeabur-plan.md` 与
> `docs/deployment/zeabur-setup.md`；最新集成账本见
> `docs/integration/zeabur-predev-integration-2026-08-02.md`。

本文件是 2026-08-01 三分支预集成的范围、来源与验证账本。它记录可接受的产品能力和明确拒绝的旧机制；不以「分支整体合并」代替逐批审计，也不把尚未执行的验证写成通过。

## 1. 基线与来源真值

`predev` 从 `yusheng/two-part-merge` 的精确提交建立；随后只有已知基线修复和按批集成提交可以进入。来源提交固定如下，短 SHA 仅作阅读提示，执行与审计一律使用完整 SHA。

| 角色            | 来源                     | 完整 SHA                                   | 用途                                                      |
| --------------- | ------------------------ | ------------------------------------------ | --------------------------------------------------------- |
| 集成基线        | `yusheng/two-part-merge` | `2cb33c9a680722968c539917c21350e34bea0815` | 当前工作流、数据与产品合同的第一真值                      |
| 前台来源        | `dev` 快照               | `d85e9ec204bf45b736b3ba4749b3545b42476edb` | 首页、Community 与部署能力候选                            |
| 后台来源        | `penguin` 快照           | `14dcfaa657249fa9921dc812cb99d0578d244c0a` | 管理后台产品能力候选                                      |
| predev 基线修复 | `predev`                 | `abdb9c6b8075cfb0a8af11bc28da1b9064de203f` | 使 job phase 合同兼容 CRLF；是 yusheng 基线的直接后续提交 |

### 1.1 只读备份引用

| 来源    | 备份分支                            | 快照 tag                              | tag peel 后的提交                          |
| ------- | ----------------------------------- | ------------------------------------- | ------------------------------------------ |
| yusheng | `backup/2026-08-01-yusheng-2cb33c9` | `snapshot/2026-08-01/yusheng-2cb33c9` | `2cb33c9a680722968c539917c21350e34bea0815` |
| dev     | `backup/2026-08-01-dev-d85e9ec`     | `snapshot/2026-08-01/dev-d85e9ec`     | `d85e9ec204bf45b736b3ba4749b3545b42476edb` |
| penguin | `backup/2026-08-01-penguin-14dcfaa` | `snapshot/2026-08-01/penguin-14dcfaa` | `14dcfaa657249fa9921dc812cb99d0578d244c0a` |

这些引用用于取证与 provenance，不授权直接 merge、覆盖或改写 `predev`。若引用与表中 SHA 不一致，立即停止该来源的集成。

## 2. 接受范围

### 2.1 dev

- 首页产品与视觉改进，但必须服从当前路由、认证、三来源项目创建和工作流合同。
- 公开 `/community`：静态 catalog，以及 `public/img/community/*`、`public/videos/community/*` 公开媒体；无需认证并进入 sitemap。
- 部署能力：容器、反向代理、构建和运维文档等候选改动，逐项核对当前环境变量与服务边界后接入。

### 2.2 penguin

- `/admin`、用户、任务、运维、安全、计费、AI 审计七个管理页面的产品能力。
- 对应 `/api/admin/*` 的 admin-only 能力与 `src/features/admin/*` 领域服务。
- 只接受基于当前 PostgreSQL schema、当前队列/attempt、当前计费与 `ai_invocations` 的真实投影；允许为这些能力做经审计的最小适配，不接受 mock 或平行真值。

## 3. 明确拒绝范围

### 3.1 penguin 不得带入

- `render_jobs` 表及其 schema、repository、投影和任何围绕它建立的管理逻辑。
- 独立 job-db、worker 队列或第二套任务状态机；任务后台必须适配 yusheng 当前 attempt、lease/ticket、execution epoch 与队列真值。
- 旧计费模型、旧额度口径或会覆盖当前双账本与资金来源合同的实现。
- penguin 的 `0019`、`0020` migration、snapshot 与 journal 记录；需要的 schema 变更必须从合并后的当前 schema 重新生成顺延迁移。

### 3.2 dev 不得覆盖

- 当前 Director、三来源启动、DAG、Artifact、render/worker、队列、recovery、ExecutionPlan、并发与双账本机制。
- 任何以 dev 的旧实现替换 yusheng 当前工作流合同的整文件覆盖、冲突一边倒或隐式回退。

出现接受能力依赖拒绝机制时，必须在当前 yusheng 合同上重做最小适配；不能以「先合进来再清理」作为放行理由。

## 4. 合并与 provenance 规则

1. 内容按责任批次审计、移植、验证和本地提交；不得直接把 dev 或 penguin 整体内容 merge 到 `predev`。
2. 每批只 stage 该批文件，提交前记录 staged 文件清单、验证命令与结果；失败或未执行必须原样保留。
3. 某来源的全部已接受内容完成后，才允许追加该来源的 provenance ours merge。该 merge 只记录祖先关系，不得改变文件树。
4. provenance merge 的第一父提交 tree hash 与 merge commit tree hash 必须完全相同。合并前可记录当前 tree 供审计；合并完成后必须以 `HEAD^1` 重新取得第一父 tree，并与 merge commit tree 比较：

   ```powershell
   git rev-parse 'HEAD^{tree}'
   git merge -s ours --no-ff <完整来源SHA>
   git rev-parse 'HEAD^1^{tree}'
   git rev-parse 'HEAD^{tree}'
   ```

5. `git rev-parse 'HEAD^1^{tree}'` 与 `git rev-parse 'HEAD^{tree}'` 的结果不同即视为失败：不得继续下一来源、不得用后续提交掩盖差异。ours merge 也不能替代内容批次的文件级 provenance、测试和审查。

## 5. 批次验证追加区

每个内容批次和 provenance 批次都在本节末尾追加一条记录。只写已经取得的证据；`未执行`、`失败`、`阻塞` 不得改写为通过。

### 批次记录模板

```markdown
### BNN · <批次名称>

- 类型：内容集成 / provenance ours merge
- 来源：<完整 SHA>
- 接受范围：<本批职责>
- 明确排除：<本批拒绝项>
- 内容提交：<commit SHA；未提交写“未提交”>
- staged 文件：<精确路径清单>
- 第一父 tree hash：<仅 provenance 批次填写；未执行写“未执行”>
- merge tree hash：<仅 provenance 批次填写；未执行写“未执行”>
- 验证：
  - `<命令>`：通过 / 失败 / 未执行（附最小证据或原因）
- 结论：通过 / 失败 / 阻塞 / 未执行
- 备注：<风险、后续动作或无>
```

### B01 · dev 营销首页与 Community 画廊

- 类型：内容集成
- 来源：`d85e9ec204bf45b736b3ba4749b3545b42476edb`；Community 内容来自 `2bc8843`，design token 修复来自 `d85e9ec`，自托管字体来自 `996516b`，standalone 浏览器打包配置来自 `712b5de`
- 接受范围：`/community` 静态 catalog 与画廊、Header/Footer Community 链接、sitemap 与 robots 合同、自托管 Geist、Next standalone Playwright runtime tracing，以及 4 张 WebP 与 4 个 MP4 公开媒体
- 明确排除：dev 旧 workflow、server、billing、deploy 文件、`.qoder`，以及对当前 `image-reveal.tsx`、OverlayRoot、移动端、reduced-motion、hydration 与既有 token 护栏的覆盖
- 内容提交：`151676e`
- staged 文件：`.gitignore`、`next.config.ts`、`package.json`、`pnpm-lock.yaml`、`public/img/community/{ai-coding-workflow,red-skill-launch,superun-product-discovery,vision-model-intro}.webp`、`public/videos/community/{ai-coding-workflow,red-skill-launch,superun-product-discovery,vision-model-intro}.mp4`、`src/app/(marketing)/community/page.tsx`、`src/app/layout.tsx`、`src/app/robots.ts`、`src/app/sitemap.ts`、`src/components/marketing/community-gallery.tsx`、`src/components/marketing/footer.tsx`、`src/components/marketing/header.tsx`、`src/features/community/catalog.test.ts`、`src/features/community/catalog.ts`、`tests/app-route-contract.test.tsx`、`tests/marketing-runtime-contract.test.ts`
- 第一父 tree hash：未执行（非 provenance 批次）
- merge tree hash：未执行（非 provenance 批次）
- 验证：
  - RED：目标 Vitest 共 3 个文件；4 个断言因 `/community` 页面、导航/站点元数据、自托管字体与 standalone Playwright 合同缺失而失败，另有 catalog 模块缺失错误
  - GREEN：目标 Vitest 3 files / 13 tests 通过
  - `pnpm test`：264 files / 1723 tests 通过；首次与架构门禁并行时两个队列导入测试超时，顺序复跑 8/8 后再顺序全量复跑通过
  - `pnpm typecheck`：通过
  - 目标文件 ESLint：通过
  - `pnpm verify:v3`：通过，`ok: true`、`violations: []`
  - `pnpm build`：通过；生产脚本固定使用 Next 16.2.11 Webpack，构建清单包含静态 `/community`
  - `node --test scripts/verify/standalone-artifact-contract.mjs`：通过；本次 `.next/standalone` 为 90,053,393 bytes / 5,346 files，Playwright、`playwright-core/browsers.json` 与 `assets/fonts` 均可从产物解析；仓库文档、测试、脚本及非白名单源码未泄漏
  - 4 个 MP4 `ffprobe`：通过；时长分别为 40.006009、57.766667、55.366667、46.766667 秒
  - 4 张 WebP 与 4 个 MP4 的 Git blob：均与锁定来源 `d85e9ec204bf45b736b3ba4749b3545b42476edb` 一致
  - `git diff --check`：通过
  - U+FFFD 扫描：通过，未发现 replacement character
  - Prettier：本批新增与实质修改的 TypeScript/TSX/JSON 文件检查通过；`.gitignore` 无可推断 parser，未将该提示写成格式通过
- 结论：通过
- 备注：此前 4 条 Next NFT warning 来自 Turbopack 对全局 Playwright broad glob 的过度追踪；生产构建已切换为 Webpack，并将显式 tracing 收窄到动态加载的 browser manifest 与字体。Webpack NFT 仍需 7 个明确白名单运行时 TypeScript 文件，独立产物合同会拒绝其余 `src` 文件。未执行真实 Chromium 页面验收，不得将本批记录解释为浏览器验收通过。

### B02 · 全局管理员访问与安全观测基础

- 类型：内容集成
- 来源：`14dcfaa657249fa9921dc812cb99d0578d244c0a` 的后台产品合同；代码按当前 yusheng schema 与认证边界重建
- 接受范围：`users.role` 全局 user/admin 角色、SessionOwner 角色投影、页面/API admin 守卫、按 route group 与安全 outcome 聚合的 API 分钟桶、显式角色 CLI 与最后活跃管理员保护
- 明确排除：penguin 的 `render_jobs`、job-db、worker 队列、旧计费查询、`0019/0020` migration/snapshot/journal，以及任何 Director、ExecutionPlan、Artifact、队列或 Provider 合同改动
- 内容提交：`44303b8`
- staged 文件：`docs/conventions/routing.md`、`package.json`、`scripts/admin/set-role.ts`、`src/features/admin/{admin-foundation.pg.test.ts,admin-role-repository.ts,admin-role-service.test.ts,admin-role-service.ts,index.ts}`、`src/features/auth/{account-service.pg.test.ts,api-access-counter.test.ts,api-access-counter.ts,api-session.test.ts,api-session.ts,auth-repository.ts,index.ts,page-session.test.ts,page-session.ts}`、`src/lib/db/migrations/pg/{0029_bored_red_ghost.sql,meta/0029_snapshot.json,meta/_journal.json}`、`src/lib/db/{schema-metadata.pg.test.ts,schema/auth.ts,schema/index.ts}`
- 第一父 tree hash：未执行（非 provenance 批次）
- merge tree hash：未执行（非 provenance 批次）
- 验证：
  - RED：3 个新测试文件因 admin guard、角色服务与 access counter 模块尚不存在而失败；随后按 HANDOFF §3 补充 outcome 出口口径测试
  - 目标 unit：4 files / 17 tests 通过
  - 目标 PostgreSQL：3 files / 27 tests 通过，覆盖默认 user、SessionOwner role、最后活跃管理员、分钟桶 outcome 聚合与 schema metadata
  - `pnpm test`：268 files / 1742 tests 通过
  - `pnpm typecheck`：通过
  - `pnpm lint`：通过
  - `pnpm verify:v3`：通过，`ok: true`、`violations: []`
  - `pnpm db:migrate` 连续两次：在 `TEST_DATABASE_URL` 上通过
  - `pnpm build`：通过，Next 16.2.11 Webpack 编译、类型检查与 20/20 静态页生成完成
  - `git diff --check`：通过
  - U+FFFD 文本扫描：通过，未发现 replacement character
- 结论：通过
- 备注：`0029` 由合并后 schema 执行 `pnpm db:generate` 生成；因手写 `0028` 没有 snapshot，Drizzle 初稿重复提出 `ai_invocations` v3 constraint，已从 `0029` SQL 中剔除，最终迁移只包含加法式 `users.role` 与 `api_access_counters`。普通 workspace owner 与 seed owner 均保持全局 `user`，不会自动获得 admin。

### B03 · 工作流安全的运营后台

- 类型：内容集成
- 来源：`14dcfaa657249fa9921dc812cb99d0578d244c0a` 的后台页面与 API 产品形状；数据服务、守卫和 UI 按当前 yusheng schema 与设计系统重建
- 接受范围：统一 Admin 横向导航壳；`/admin`、`/admin/jobs`、`/admin/ops`、`/admin/security`、`/admin/ai`；登记的 DAU、任务、运维、安全和 AI admin API；`pipeline_runs + task_attempts` 安全任务投影；queue、lease、dispatch、cooldown、Provider pool 只读观测；`ai_invocations v3` 与双账本成本聚合
- 明确排除：penguin `render_jobs`、job-db、worker 队列、旧错误原文、旧计费查询、第二套控制面，以及对 ExecutionPlan、DAG、Artifact、队列、Provider 预算或凭据的任何写入
- 内容提交：`484d93ea06489b0f431fa5a6b924b9ec9920008c`
- staged 文件：`docs/conventions/routing.md`、`docs/designs/Design-system-inventory.md`、`src/app/admin/{layout.tsx,page.tsx,ai/page.tsx,jobs/page.tsx,ops/page.tsx,security/page.tsx}`、`src/app/api/admin/{ai/route.ts,jobs/route.ts,jobs/[id]/route.ts,metrics/dau/route.ts,ops/route.ts,security/route.ts}`、`src/features/admin/{ai-audit-repository.ts,ai-audit.pg.test.ts,http-query.ts,index.ts,jobs-repository.ts,metrics-repository.ts,operational-dashboards.pg.test.ts,operational-projections.test.ts,operational-projections.ts,ops-repository.ts,overview-repository.ts,security-repository.ts}`、`src/features/admin/ui/{admin-shell.tsx,ai-audit-panel.tsx,jobs-panel.tsx,ops-panel.tsx,security-panel.tsx}`、`tests/admin-operational-route-contract.test.ts`
- 第一父 tree hash：未执行（非 provenance 批次）
- merge tree hash：未执行（非 provenance 批次）
- 验证：
  - RED：目标 unit 因投影模块、5 个页面与登记 API 尚不存在而失败；随后按 routing SSOT 撤销未登记 `/api/admin/overview`，改锁 `/api/admin/metrics/dau` 与 `/api/admin/jobs/[id]`
  - 目标 unit：2 files / 14 tests 通过，覆盖最小字段、错误类别脱敏、AI allowlist、页面/API 守卫与路由形状
  - 目标 PostgreSQL：2 files / 3 tests 通过，覆盖 overview/security 聚合、DAU、run/attempt 详情、租约/dispatch/cooldown/pool 观察、AI v3 与 `official_cost_entries + entitlement_ledger_entries` 成本真值
  - `pnpm test:pg`：53 files / 303 tests 通过
  - `pnpm test`：首次仅 `src/lib/queue/init.test.ts` 的 globalThis 单例动态 import 超过 5 秒；该文件隔离复跑 7/7 通过，随后顺序全量复跑 270 files / 1756 tests 通过
  - `pnpm lint`：通过
  - `pnpm typecheck`：通过
  - `pnpm verify:v3`：通过，`ok: true`、`violations: []`
  - `pnpm verify:workflow`：通过，blocking violations 为 0；历史时间戳倒序 1702 条保持 advisory，本批未修改工作流写路径
  - `pnpm build`：通过；Next 16.2.11 Webpack 编译、类型检查与 20/20 静态页生成完成，route manifest 仅包含 routing SSOT 登记的 admin 页面和 API
  - `git diff --check`：通过
  - Prettier：本批文本与 TypeScript/TSX 文件检查通过
  - U+FFFD 扫描：通过，未发现 replacement character
- 结论：通过
- 备注：账号与计费模块仍属于下一内容批次，Admin 导航以「待接线」禁用文本呈现，不提供空链接。未在本批启动服务或执行真实 HTTP/Chromium 截图；这些仍是最终 UI 验收项，不得据此宣称浏览器验收通过。

### B04 · 运营后台合同审查修正

- 类型：审查修复
- 来源：B03 的规格审查反馈；不新增 penguin 运行机制或后台写路径
- 修正范围：删除第二套 `AdminShell` 和 pathname/active 映射，`admin/layout.tsx` 只保留鉴权、metadata 与 children；各页改用无激活态的页面级 `AdminPageFrame`；AI 双账本缺失或 NULL 成本保持“未知”并暴露已知小计、登记覆盖率与 measurement quality；运维投影只统计当前 queued、租约未过期的 running、有效 waiting/active lease、有效 scheduled/in-flight ticket 与当前 cooldown；`auth_throttle` 对齐最近 24 小时窗口；两份权威文档恢复到 `40fbac3` 原格式后仅重放语义变更
- 明确排除：第二套应用壳、Sidebar、pathname 高亮模型；将缺失成本伪装为 0；把 released、终态或过期记录混入当前运维状态；格式化器造成的 Markdown 表格重排
- 修复提交：`825dffc760c9465e593fe55d06e4b5e07086150c`
- staged 文件：`docs/conventions/routing.md`、`docs/designs/Design-system-inventory.md`、`src/app/admin/{layout.tsx,page.tsx,ai/page.tsx,jobs/page.tsx,ops/page.tsx,security/page.tsx}`、`src/features/admin/{ai-audit-repository.ts,ai-audit.pg.test.ts,operational-dashboards.pg.test.ts,operational-projections.test.ts,operational-projections.ts,ops-repository.ts,security-repository.ts}`、`src/features/admin/ui/{admin-page-frame.tsx,admin-shell.tsx（删除）,ai-audit-panel.tsx}`、`tests/admin-operational-route-contract.test.ts`
- 第一父 tree hash：未执行（非 provenance 批次）
- merge tree hash：未执行（非 provenance 批次）
- 验证：
  - RED：新增合同测试分别证明第二套 Admin 壳、AI 成本归零、历史运维状态与超窗 auth throttle 会失败
  - 目标 unit：2 files / 14 tests 通过
  - 目标 PostgreSQL：2 files / 3 tests 通过，覆盖 NULL/缺失账本、coverage/quality、历史与过期运维记录排除、24 小时限流窗口
  - `pnpm typecheck`：通过
  - scoped ESLint：通过，`src/features/admin`、`src/app/admin`、`src/app/api/admin` 与目标路由合同测试无 warning
  - `pnpm verify:v3`：通过，`ok: true`、`violations: []`
  - `git diff --check`：通过
  - U+FFFD 扫描：通过，未发现 replacement character
  - 文档噪音审计：相对 `40fbac3`，`routing.md` 仅 12 行替换，`Design-system-inventory.md` 仅新增 11 行
- 结论：通过
- 备注：B03 中“统一 Admin 横向导航壳”的记录由本条修正为页面级轻量框架，不再存在独立 Admin shell。未执行真实 HTTP/Chromium，本项仍留给最终 UI 验收。

### B05 · 运营指标与跨 workspace 查询加固

- 类型：审查修复 + 加法式迁移
- 来源：B03/B04 的质量复审；不新增后台写路径或第二套遥测真值
- 修正范围：兼容保留 `/api/admin/metrics/dau` 路径，但 repository 与 payload 改为 `metric=last_session_activity` 的最后会话活动分布，并明确 `snapshotNature=mutable`、`historicalDau=false`；同一 session 的 `last_seen_at` 跨天更新后会离开旧日桶。任务失败分类改为由当前 `WorkflowFaultCode | WorkflowExecutionNotice.code` 编译期穷尽的显式 allowlist，`TASK_INTERRUPTED` 投影为 `cancelled`、`PROVIDER_POOL_WAIT` 投影为 `capacity`、未知 code 投影为 `internal`。Drizzle 生成 `0030_steady_venus`，只增加 `ai_invocations(telemetry_version, created_at DESC NULLS LAST)` 与 `task_attempts(status, created_at DESC NULLS LAST)` 两条跨 workspace 管理查询索引
- 明确排除：把可变 `sessions.last_seen_at` 快照宣称为历史 DAU；按 raw message、kind 子串或正则猜测错误类别；回显原始错误；修改旧迁移、旧 snapshot 或复活 penguin `0019/0020`
- 修复提交：`e87f35cbd8330e9b808d0c0ebde651f20287e36b`
- staged 文件：`docs/conventions/routing.md`、`src/app/api/admin/metrics/dau/route.ts`、`src/features/admin/{index.ts,metrics-repository.ts,operational-dashboards.pg.test.ts,operational-projections.test.ts,operational-projections.ts}`、`src/lib/db/schema/{ai.ts,execution.ts}`、`src/lib/db/schema-metadata.pg.test.ts`、`src/lib/db/migrations/pg/{0030_steady_venus.sql,meta/0030_snapshot.json,meta/_journal.json}`、`tests/admin-operational-route-contract.test.ts`
- 第一父 tree hash：未执行（非 provenance 批次）
- merge tree hash：未执行（非 provenance 批次）
- 验证：
  - RED：unit/route 3 个预期失败，证明旧 metrics 函数名与 `PROVIDER_POOL_WAIT`/`TASK_INTERRUPTED` 错误分类不符合合同；PG 3 个预期失败，证明新 metrics 尚不存在、权威 code 未映射且两条索引缺失
  - 目标 unit：3 files / 19 tests 通过，包含 migration journal 合同
  - 目标 PostgreSQL：2 files / 9 tests 通过，覆盖同一 session 跨天更新后旧日桶归零及两条索引名称、列顺序、DESC/NULLS LAST metadata
  - `pnpm db:migrate`：连续执行两次均成功，首次应用 `0030`、第二次幂等
  - `pnpm typecheck`：通过
  - `pnpm lint`：通过
  - `pnpm verify:v3`：通过，`ok: true`、`violations: []`
  - `git diff --check`：通过
  - U+FFFD 扫描：通过，未发现 replacement character
  - `pnpm test:pg`：在 364 秒工具上限退出 124，未返回完成统计；不得记为通过。目标 PG 已独立通过，完整 PG 仍待后续核销
- 结论：目标修复与迁移验收通过；完整 `pnpm test:pg` 未完成
- 备注：B03 中“DAU”表述由本条更正为最后会话活动的可变快照；路径中的 `dau` 仅为兼容名称，不代表指标语义。未执行真实 HTTP/Chromium。

### B06 · 默认任务列表查询与 top-N 索引收口

- 类型：审查修复 + 加法式迁移
- 来源：B03/B05 的默认任务列表性能复审；不新增任务表、任务状态或后台写路径
- 修正范围：默认任务列表改由 `task_attempts` 驱动并 `INNER JOIN pipeline_runs`，只投影真实 attempt 与所属 run，避免从 `pipeline_runs` 以 `LEFT JOIN` 扫描无 attempt 的 run；单条详情继续由 `pipeline_runs` 驱动并保留 `LEFT JOIN`，因此仍能诊断尚未创建 attempt 的 run。Drizzle 生成 `0031_tiny_spencer_smythe`，只增加 `task_attempts(created_at DESC NULLS LAST)` 全局索引，并保留 `task_attempts(status, created_at DESC NULLS LAST)` 复合索引
- 明确排除：删除无 attempt run 的详情可观测性；修改工作流状态、DAG、队列、Artifact、ExecutionPlan 或旧迁移；用数据库参数改变生产规划器行为
- 修复提交：`9677d83b024b50197b388896c623946ec6571acb`
- staged 文件：`src/features/admin/jobs-repository.ts`、`src/features/admin/operational-dashboards.pg.test.ts`、`src/lib/db/migrations/pg/{0031_tiny_spencer_smythe.sql,meta/0031_snapshot.json,meta/_journal.json}`、`src/lib/db/schema-metadata.pg.test.ts`、`src/lib/db/schema/execution.ts`、`tests/admin-operational-route-contract.test.ts`
- 第一父 tree hash：未执行（非 provenance 批次）
- merge tree hash：未执行（非 provenance 批次）
- 验证：
  - RED：route 合同因列表仍由 `pipeline_runs LEFT JOIN` 驱动而失败；PostgreSQL 行为测试证明列表错误包含无 attempt 的 run；默认 top-N `EXPLAIN` 使用 identity 索引并出现显式 Sort；schema metadata 证明全局 created-at 索引缺失
  - 目标 unit：2 files / 15 tests 通过，包含列表/详情 join 方向与 migration journal 合同
  - 目标 PostgreSQL：2 files / 10 tests 通过，覆盖无 attempt run 不进入列表但仍可由详情查询、全局/复合索引 metadata，以及默认无 status top-N 查询使用 `task_attempts_admin_created_idx` 且无显式 Sort
  - `EXPLAIN` 说明：测试会话仅设置 `enable_seqscan=off` 以稳定证明索引结构合同，随后恢复；该设置不进入应用或生产配置
  - `pnpm db:migrate`：连续执行两次均成功，首次应用 `0031`、第二次幂等
  - `pnpm typecheck`：通过
  - `pnpm lint`：通过
  - `pnpm verify:v3`：通过，`ok: true`、`violations: []`
  - `git diff --check`：通过
  - U+FFFD 扫描：通过，未发现 replacement character
  - `pnpm test:pg`：本批未重新执行全量；B05 的完整 PG 仍待后续核销
- 结论：默认任务列表、详情兼容与索引结构合同通过目标验收；完整 `pnpm test:pg` 未完成
- 备注：本批未执行真实 HTTP/Chromium；数据库查询行为与计划结构已由目标 PostgreSQL 测试锁定。

### B07 · 安全账号与计费管理

- 类型：功能集成
- 来源：`penguin@14dcfaa657249fa9921dc812cb99d0578d244c0a` 的用户/计费后台产品形状；实现按当前 yusheng 认证、权益、兑换与双账本合同重建
- 接受范围：`/admin/users`、`/admin/billing` 与 routing 登记的五组 API；原子创建 user + workspace + owner + Free；账号编辑、可逆停用/恢复和会话注销；方案 manifest、Workspace 权益、双账本覆盖率与兑换批次投影；一次性明文兑换码和按批次撤销
- 明确排除：物理删除用户或业务数据、owner 自动升级 admin、旧计费表/额度口径、`render_jobs`、第二套任务/队列、Provider 原始成本猜测，以及任何 ExecutionPlan、DAG、Artifact 或工作流写入
- 实现提交：`d1dd00a09cd241eded190657f487f1bfaa7789eb`
- staged 文件：routing/design SSOT；admin users/billing 页面、API、服务、公共 Admin 导航与 UI；兑换码生成器；目标 unit/PG/route 合同测试
- 第一父 tree hash：未执行（非 provenance 批次）
- merge tree hash：未执行（非 provenance 批次）
- 验证：
  - RED：route 合同 8/8 因入口缺失而失败；PG 4/4 因 user-admin / billing-admin 模块缺失而失败；后续审查 RED 锁住 ILIKE 通配转义、无真实计数的 mutation 响应、nullable 双账本成本和无效截止时间拒绝
  - 目标 unit：2 files / 10 tests 通过；目标 PostgreSQL：1 file / 5 tests 通过
  - PostgreSQL 覆盖：原子账号创建、自停用拒绝、并发最后管理员保护、停用注销全部会话且保留 Workspace、恢复、`%_` 字面搜索、明文只在创建响应出现、DB 仅 HMAC、后续投影无明文、批次撤销后真实兑换返回 unavailable
  - `pnpm lint`、`pnpm typecheck`、`pnpm verify:v3`、`pnpm verify:workflow`、`pnpm build`：通过；workflow blocking findings 为 0
  - `pnpm db:migrate`：连续执行两次成功；本批没有 schema 或 migration 变化
  - `git diff --check` 与 U+FFFD 扫描：通过
  - `pnpm test`：两次全量分别因 3 个、2 个既有高负载测试并行超时而未全绿；本批 hydration 失败已通过显式 UTC 修复，所有报告失败文件分别聚焦复跑 3 files / 13 tests 与 2 files / 8 tests 均通过
  - `pnpm test:pg`：本批未重新执行全量；目标 PG 已独立通过
- 结论：本批代码、目标数据库行为、架构、工作流对账与生产构建通过；全量 unit / PG 和真实 HTTP / Chromium 仍由最终综合验收核销，不得据此宣称已完成浏览器验收。

### B08 · 跨入口全局管理员锁收口

- 类型：规格复审修复
- 来源：B07 的并发安全复审；不扩大后台能力或数据模型
- 修正范围：账号停用与 `admin:set-role` 降权统一取得同一 PostgreSQL advisory transaction lock，保证所有可能减少活跃全局管理员数量的入口串行检查“最后一个管理员”不变量
- 明确排除：改变角色定义、自动授予 admin、物理删除用户、扩大 CLI 权限或修改认证会话合同
- 修复提交：`c4b48ae6c8336779272cc578d5fed3921ca5bd89`
- staged 文件：`src/features/admin/{global-admin-mutation-lock.ts,user-admin.ts,admin-role-repository.ts,account-billing.pg.test.ts}`
- 验证：
  - RED：使用独立两连接 PostgreSQL pool 与 UPDATE 延迟触发器稳定复现账号停用和 CLI 降权同时通过，最终留下 0 个活跃 admin
  - GREEN：2 PG files / 9 tests 通过；跨入口竞态中恰好一个操作更新，另一个返回 `LAST_ACTIVE_ADMIN` / `last_active_admin`，最终恰有 1 个活跃 admin
  - `pnpm typecheck`、scoped ESLint、`pnpm verify:v3`、`git diff --check` 与 U+FFFD 扫描：通过
- 结论：跨入口 lockout 竞态已由单一共享锁与真实并发 PG 回归锁住。

### B09 · 管理写请求边界加固

- 类型：质量复审修复
- 来源：B07 管理写 API 的请求边界复审；不新增后台能力或路由
- 修正范围：全部 admin POST / PATCH / DELETE 统一要求 `application/json`（允许 charset）；浏览器请求的 Origin 必须与请求 URL 同源，`Sec-Fetch-Site` 只允许 `same-origin | none`；未携带浏览器元数据的可信 server / CLI JSON 请求继续允许；用户与兑换批次 path id 在服务和数据库调用前统一校验 UUID
- 明确排除：信任 `same-site`、允许跨 Origin、从无效 id 查询数据库、改变 admin 会话守卫或业务错误语义
- 修复提交：`afce1c1c7ae41789ae90b27888c9f6f92d8bf5d3`
- staged 文件：四个 admin mutation route 文件、`src/features/admin/http-errors.ts`、`mutation-request.test.ts` 与 admin route contract 测试
- 验证：
  - RED：既有写入口接受 text/plain、foreign Origin、same-site Fetch 且继续调用 service；path id 未经 UUID 校验进入 service
  - GREEN：2 files / 20 tests 通过，覆盖 text/plain 415、foreign Origin 403、same-site Fetch 403、same-origin JSON + charset 通过，以及非法 user / batch UUID 400 且 service 调用数为 0
  - `pnpm typecheck`、scoped ESLint、`pnpm verify:v3`、`git diff --check` 与 U+FFFD 扫描：通过
- 结论：admin 写入口统一在业务服务前完成媒体类型、浏览器来源与资源标识校验。

### B10 · 不可变部署批次运行时验收

- 类型：部署镜像与 Compose 静态验收；未启动正式 Compose，未连接任何开发数据库、凭据或持久卷
- 来源：`predev@1a00ee9`（`fix(deploy): bootstrap Debian HTTPS certificates`）；本次只验收既有本地镜像 `purpleink-{web,worker,migrate}:verify-predev`
- 镜像标识（脱敏）：Web `sha256:b761edc6172…550cd811`、Worker `sha256:db61a1c7e28e…3de61001`、Migrate `sha256:160f54531a4…fe9410a`
- 接受范围：Web/Worker 非 root 与真实 Chromium 启动；Worker 工作目录、媒体二进制与输出目录；三镜像不带仓库的 `docs/`、`tests/`、`.env*`；Migrate 只带迁移闭包；生产 Compose 的公开端口与网络边界
- 明确排除：启动 `docker compose`、数据库迁移、完整 HTTP 健康检查、浏览器业务路径、卷写入/重启持久化、外部 DNS/TLS 与真实凭据验收
- 验证（全部命令均无环境变量值或凭据输出）：
  - `docker image inspect purpleink-{web,worker,migrate}:verify-predev`：Web 与 Worker `Config.User=pwuser`；Worker `Config.WorkingDir=/repo/server`；三个镜像均使用指定 `verify-predev` tag
  - `docker run --rm --entrypoint sh purpleink-web:verify-predev -c '<UID/目录边界断言>'`：UID `999`、用户 `pwuser`；`/app/docs`、`/app/tests`、`/app/.env*` 均不存在
  - `docker run --rm --entrypoint sh purpleink-worker:verify-predev -c '<UID、pwd、ffmpeg、ffprobe、目录边界断言>'`：UID `999`、用户 `pwuser`、`pwd=/repo/server`；`ffmpeg -version` 与 `ffprobe -version` 成功；`/repo/server/out` 与 `/repo/server/capture` 存在；仓库 `docs/`、`tests/` 与 `.env*` 均不存在
  - `docker run --rm --entrypoint node purpleink-{web,worker}:verify-predev -e '<launch Chromium, setContent, close>'`：两张镜像均输出 `*-chromium-launch=ok`，证明实际启动并关闭 Linux Chromium，而不是只检查二进制文件
  - `docker run --rm --entrypoint sh purpleink-migrate:verify-predev -c '<精确文件树断言>'`：迁移入口、`src/lib/db/migrate.ts`、schema 与 PostgreSQL migrations 存在；顶层严格限于依赖、锁文件、迁移脚本/源码及 tsconfig；`server/` 与 `packages/` 仅保留各自 `package.json` 和 pnpm 生成的 TSX/Playwright shim，不包含 worker/app 源码、文档、测试或 `.env*`
  - Docker Compose `v5.1.3` 下分别执行 `docker compose -f deploy/compose.yaml config --format json --no-interpolate` 与 `docker compose -f docker-compose.prod.yml config --format json --no-interpolate`：两者解析拓扑一致，项目名均为 `purpleink`；仅 `caddy` 声明 published `80,443` 并连接 `edge,app`，`web` 连接 `app,data`，`worker` 严格为 `app`，`migrate` 与 `postgres` 严格为 `data`。后者是带 `name: purpleink` 与 `include: ./deploy/compose.yaml` 的兼容入口，因此命名网络/卷同样解析为 `purpleink_*`，不是第二套 production stack。
- 备注：外部 Debian HTTP 源在构建期间不稳定；镜像现在先以 HTTP 安装 `ca-certificates`，再将 Debian 源替换为 HTTPS 并继续安装字体/运行依赖，同时设置 APT 重试与超时。这是 HTTPS/CA bootstrap 修复，不是一次生产网络可用性声明。完整 Compose HTTP 与卷/数据库持久化仍未执行，必须在隔离验收环境中完成。
- 复核说明：旧 `PLAN-001` 与 ISSUE-015 历史证据中的 `reverse-proxy` 唯一 `443` 属于已明确标注为 historical 的旧合同；当前可执行真值为 `deploy/compose.yaml`、`deploy/Caddyfile` 与 `docs/deployment/`。兼容入口此前未显式设置项目名，可能按调用目录派生为第二套卷/网络；已由 `fix(deploy): pin compatibility project name` 添加 `name: purpleink`，并以实时 `config` 解析和 `tests/deployment-config.test.ts`（14/14）验证当前 80/443、三网络边界和统一 `purpleink_*` 命名空间。
- 结论：本批限定的本地镜像运行时与 Compose 静态边界通过；未执行项目必须继续保持未验收状态。
### B11 — isolated HTTP and Chromium acceptance harness

- Type: final-acceptance automation; it is intentionally isolated from the
  existing `purpleink-dev-postgres-1` Compose project and any developer data.
- Added: `scripts/verify/predev-browser/predev-browser.ts`, its temporary
  Compose override, an internal-TLS Caddy config, and focused Vitest contracts.
  Each run generates a nonce-scoped `purpleink_predev_<nonce>` project, fresh
  named volumes, a temporary ignored env file, loopback-only random Caddy and
  Postgres ports, and one-time secrets. Evidence is redacted and written only
  to ignored `.data/integration-evidence/<commit>/`.
- Static proof: the composed configuration resolves to Caddy as the only
  published service on `127.0.0.1:<random>`, Postgres on its own random
  loopback port, and nonce-scoped `edge`, `app`, `data`, and volume names.
  `pnpm exec tsc --noEmit`, `pnpm exec vitest run
  tests/predev-browser-contracts.test.ts` (4/4), and `git diff --check` passed.
- Runtime attempt: blocked before any service, migration, seed, HTTP request,
  Chromium session, or model call. Local image build stalled at the Web runtime
  `node /playwright/node_modules/playwright/cli.js install chromium` step;
  BuildKit reported a mutable 0 B cache mount and no
  `purpleink_predev_<nonce>` container was created after the bounded wait.
  The Docker client was stopped, then the exact project received `down
  --volumes --remove-orphans`; a follow-up list found no matching containers or
  volumes, and the temporary env file was deleted.
- Conclusion: the harness and its static isolation contract are verified. The
  runtime acceptance matrix (two idempotent migrations, seeding/promotion,
  archive baseline, media/Range/ffprobe, admin mutation cases, redaction,
  restart persistence, and browser screens) remains **blocked**, not passed.
  No paid model was invoked or claimed as passed.

### B12 — acceptance harness hardening follow-up

- The isolated Compose contract now permits exactly two loopback published
  ports: Caddy HTTPS (`443`) and the temporary Postgres port (`5432`).  Every
  resolved network and volume must use the generated `purpleink_predev_<nonce>`
  prefix; a focused contract test locks this rule.
- No-build is now conditional on all three candidate images carrying an exact
  `org.opencontainers.image.revision` matching the current `HEAD`. Existing
  `*-verify-predev` images had no labels, so the runner correctly selected
  `build`, rather than trusting their mutable-looking tags. Dockerfiles now
  write the revision label from the build argument for future attested images.
- The runner checkpoints a redacted manifest at every phase and redacts each
  generated database password, account password, one-time redemption code, and
  captured failure text before evidence is written. It also implements the
  disabled-account old-session invalidation/restore-new-login flow, self-disable
  guard, and generated-secret scans of jobs/ops/AI/billing responses. These
  behavioral checks were not reached in the run below.
- Runtime retry: again blocked in `docker compose up --build --wait` before a
  container existed. The prior interrupted Compose Buildx client was also found
  still building the same Chromium layer; both exact temporary projects were
  stopped, then no matching process, container, volume, or temporary env file
  remained. This is a build-environment blocker, not evidence for migrations,
  archive baseline, admin guards, media, restart persistence, or Chromium.
- Conclusion: static hardening and target tests passed; runtime acceptance
  remains **blocked**. In particular, do not read the newly implemented checks
  as executed acceptance, and no paid model was called.
